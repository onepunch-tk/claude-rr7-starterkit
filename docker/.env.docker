# ================================================================================
# Docker 컨테이너용 환경 변수
# ================================================================================
#
# 이 파일은 Docker 컨테이너에서 호스트 머신의 Supabase 로컬 DB에 접근할 때 사용합니다.
#
# 사용법:
#   docker compose -f docker/docker-compose.yml --env-file docker/.env.docker up
#
# 또는 docker run 시:
#   docker run -p 3000:3000 --env-file docker/.env.docker claude-rr7-starterkit
#
# ================================================================================

# ⭐ PostgreSQL 데이터베이스 URL (호스트 머신의 Supabase 로컬 DB)
# host.docker.internal: Docker 컨테이너에서 호스트 머신을 가리키는 특수 DNS
# 주의: Linux에서는 --add-host=host.docker.internal:host-gateway 옵션 필요할 수 있음
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:54322/postgres

# ⭐ 애플리케이션 기본 URL
BASE_URL=http://localhost:3000

# ⭐ Better-auth Secret (필수!)
# 실제 사용 시 반드시 변경하세요!
BETTER_AUTH_SECRET=your-secret-key-here-change-in-production

# ================================================================================
# OAuth 프로바이더 설정 (필요 시 설정)
# ================================================================================

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# ================================================================================
# Resend 이메일 서비스 설정
# ================================================================================

RESEND_API_KEY=
RESEND_FROM_EMAIL=claude-rr7@resend.dev
