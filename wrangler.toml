#:schema node_modules/wrangler/config-schema.json

# ⭐ 프로젝트 이름 (영문, 숫자, 하이픈만 사용 가능)
# 예: my-awesome-app
name = "claude-rr7-starterkit"

compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

main = "./build/server/index.js"
assets = { directory = "./build/client" }

[build]
command = "bun run build"

# ⭐ Production 환경 설정
# 배포 URL: https://[name].workers.dev
[env.production]
name = "claude-rr7-starterkit-production"

# ⭐ Staging 환경 설정
# 배포 URL: https://[name].workers.dev
[env.staging]
name = "claude-rr7-starterkit-staging"

# 💡 환경 변수 설정 방법:
# bunx wrangler secret put VARIABLE_NAME --env production
# bunx wrangler secret put VARIABLE_NAME --env staging
#
# 필수 환경 변수:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - DATABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
